apiVersion: v1
kind: Service
metadata:
  annotations:
  labels:
    app: mysqldb
  name: mysqldb
  namespace: dev-travel-agency
spec:
  ports:
  - name: tcp
    port: 3306
    protocol: TCP
    targetPort: 3306
  selector:
    app: mysqldb
